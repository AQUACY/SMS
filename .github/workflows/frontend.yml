name: Deploy Frontend to Shared Hosting

# This workflow deploys the Quasar/Vue frontend to shared hosting via FTPS
# It builds the frontend with the production API URL and deploys to /public_html
# No SSH access required - uses FTPS (FTP over SSL/TLS) for secure transfer

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'
  workflow_dispatch: # Allow manual trigger from GitHub Actions UI

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Checkout the repository code
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      # Step 3: Install dependencies
      - name: Install npm dependencies
        run: |
          cd frontend
          npm ci
      
      # Step 4: Build frontend with production API URL
      # The API URL is read from VITE_API_URL environment variable
      # Vite automatically loads .env.production, but env vars take precedence
      - name: Build frontend
        run: |
          cd frontend
          echo "Building with VITE_API_URL: $VITE_API_URL"
          npm run build
        env:
          NODE_ENV: production
          QUASAR_MODE: spa
          # VITE_API_URL will be used by Vite to replace import.meta.env.VITE_API_URL
          VITE_API_URL: ${{ secrets.FRONTEND_API_URL || 'https://sms.kdgsolution.com/api' }}
      
      # Step 5: Verify API URL in built files
      - name: Verify API URL in build
        run: |
          cd frontend/dist/spa
          echo "Checking for API URL in built files..."
          PROD_URL="${{ secrets.FRONTEND_API_URL || 'https://sms.kdgsolution.com/api' }}"
          
          # Check for localhost (should NOT be found)
          if grep -r "localhost:8000" . 2>/dev/null; then
            echo "⚠️  WARNING: Found localhost:8000 in built files!"
            echo "This means the API URL replacement didn't work correctly."
            exit 1
          else
            echo "✅ No localhost found - API URL was correctly replaced"
          fi
          
          # Check for production URL (should be found)
          if grep -r "${PROD_URL}" . 2>/dev/null; then
            echo "✅ Found production API URL (${PROD_URL}) in built files"
          else
            echo "⚠️  WARNING: Production API URL not found in built files"
            echo "Searching for any API URL patterns..."
            grep -r "https://" . 2>/dev/null | head -5 || echo "No https URLs found"
          fi
        continue-on-error: true
      
      # Step 6: Deploy built files to /public_html
      # The Quasar build outputs to dist/spa/ directory
      - name: Deploy frontend to /public_html
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps # Use FTPS (FTP over SSL/TLS) for secure file transfer
          # Alternative protocols if FTPS doesn't work: "ftp" or "ftps-legacy"
          port: ${{ secrets.FTP_PORT || 21 }} # FTPS default port is 21 (or 990 for explicit FTPS)
          local-dir: ./frontend/dist/spa/
          server-dir: /public_html/app/
          exclude: |
            **/.git*
            **/.git*/**
            **/.DS_Store
            **/Thumbs.db
      
      # Step 7: Deployment summary
      - name: Deployment summary
        run: |
          echo "✅ Frontend deployment completed successfully!"
          echo ""
          echo "🌐 Frontend files deployed to: /public_html/sms/"
          echo "🔗 API URL configured: ${{ secrets.FRONTEND_API_URL || 'https://sms.kdgsolution.com/api' }}"
          echo ""
          echo "⚠️  Important post-deployment steps:"
          echo "   1. Verify the API URL is correct in the deployed files"
          echo "   2. Ensure your domain points to /public_html/sms/"
          echo "   3. Check that .htaccess file exists for Vue Router history mode"
          echo "   4. Test the application in a browser"

